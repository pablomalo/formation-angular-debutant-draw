export class SizeConstants {
  static readonly CANVAS_HEIGHT = 600;
  static readonly CANVAS_WIDTH = 800;
  static readonly MIN_CANVAS_OVERLAP = 5; // A new shape must have at least 5px on the canvas, otherwise it might be invisible.
}
