import { ButtonActiveDirective } from './button-active.directive';

describe('ButtonActiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonActiveDirective();
    expect(directive).toBeTruthy();
  });
});
